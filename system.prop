# Identity
ro.product.model=Redmi Note 14 Pro+
ro.product.device=amethyst

# Lags & Smoothness
persist.sys.binary_xml=true
ro.shell.disable_ext_prof=true

# Display Density (Ito ang kailangan ng Framework)
ro.sf.lcd_density=440

# USB Config
persist.sys.usb.config=mtp,adb
